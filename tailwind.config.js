// tailwind.config.js
export default {
  darkMode: 'class', // Aktifkan dark mode dengan class 'dark'
  theme: {
    extend: {
      colors: {
        primary: '#3B82F6', // Biru Tailwind default (ganti sesuai branding)
        secondary: '#F59E0B', // Kuning/Orange
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'], // Gunakan font Inter
      },
    },
  },
  plugins: [],
};
